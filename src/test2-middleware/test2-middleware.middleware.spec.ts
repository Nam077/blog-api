import { Test2MiddlewareMiddleware } from './test2-middleware.middleware';

describe('Test2MiddlewareMiddleware', () => {
  it('should be defined', () => {
    expect(new Test2MiddlewareMiddleware()).toBeDefined();
  });
});
